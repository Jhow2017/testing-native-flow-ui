export type ThemeType = {
    colors: {
        primary: string;
        secondary: string;
        // Adicione todas as suas cores aqui
    };
    fontSize: {
        small: number;
        medium: number;
        large: number;
    };
    fontFamily: {
        primary: string;
        secondary: string;
    };
    // E assim por diante
};
